import { MyMatchHeightDirective } from './my-match-height.directive';

describe('MyMatchHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new MyMatchHeightDirective();
    expect(directive).toBeTruthy();
  });
});
